"""Cursor (IDE + CLI) agent adapter package.

Placeholder for now: this task only adds the read-only SQLite access layer
(``scripts.agents.cursor.store``). The ``CursorAdapter`` class satisfying
the shared ``AgentAdapter`` protocol lands in a later task, mirroring how
``scripts.agents.claude``/``scripts.agents.codex`` are structured.
"""
